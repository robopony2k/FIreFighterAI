# Firefighter AI - Game Design Reference

Purpose: A concise, editable reference for the overall game loop, systems, and intended player experience. This document is meant to evolve with the design.
Story: You are the new "Fire Warden" in charge of a region. Your mission is to protect and serve the people throughout your career. Will you be the greatest fire warden of all time and a true hero, or will your leadership be so inept that you are fired for incompetence?

## High-Level Vision

- A strategic fire-response simulation with long-term planning and tactical firefighting.
- Climate drives the world instead of fixed seasons; "seasons" are emergent from temperature and moisture.
- The player manages a 20-year campaign where risk gradually increases due to climate change.
- Towns are terrain-aware settlements that start from seeded street skeletons, form compact blocks when terrain allows, and keep growing over the campaign, increasing building exposure and fire risk over time. Map generation precomputes each town's ideal 20-year expansion queue, records replayable prerequisite road paths, and bakes the required future house-pad terrain into the day-1 landform, while runtime approval and recovery pressure decide how quickly queued expansion lots and their prerequisite roads become active. Runtime construction does not reshape terrain; buildings adapt to the static landform through structure grounding and foundations.
- Balance the competing demands of managing a budget, the firefighters under your command, and the community's preparedness.
- The game should feel readable and decisive: clear cause and effect, no hidden "gotchas."

## Design Pillars

- Tactical clarity: players can read why a fire did or did not spread.
- Strategic tension: prevention and response both matter.
- Forecast-driven play: climate outlook guides risk-taking and investment.
- Long arc progression: meaningful growth across a 20-year career.

## Pacing Target

- 20 years should feel like a single run (target: ~30 minutes real time).
- The calendar is continuous; climate creates "virtual seasons."

## Core Player Loop

1) Observe conditions (forecast, wind, risk).
2) Deploy resources and plan containment.
3) React to fires in real time.
4) End-of-year review and budget adjustments.
5) Invest in training, recruitment, and preventative measures.

## Campaign Timeline

- 20 years total.
- No hard season gates in the final design; climate conditions define the current "seasonal mode."
- Year progression should scale difficulty through climate trends, ignition frequency, and unlock cadence.

## Climate and Weather

- Daily temperature and moisture drive ignition and spread.
- Weather forecast provides a rolling 90-day outlook of fire risk and overlays deterministic seeded autumn rain periods as blue bands so players can see the upcoming weather-clear window.
- A year is 360 days with four distinct seasons: (Winter = cold + moist, Spring = warm + moist, Summer = warm + dry, Autumn is Cool + dry)
- Climate change is represented by a warming trend and drying bias.
- Each year has one deterministic, seed-jittered mid-autumn rain event. It is brief and reads as a 3D ShaderToy-inspired weather front: bounded ray-marched storm clouds in the seasonal sky plus screen-space rain aligned to wind direction relative to the camera, with wet dimming, mist, and sheen. Two coherent generated inputs span all seasons: a cellular 2D weather field defines large separated footprints and per-cell growth, while a padded 32³ RGBA atlas supplies continuous trilinearly interpolated Perlin-Worley bodies, rounded billows, and two erosion scales. Smooth morphology profiles give summer and spring fewer large cumulus with firm bases, widening bodies, cauliflower crowns, and broad blue-sky gaps; autumn uses a lighter broken intermediate stratocumulus layer; winter forms a lower, deeper, darker broken storm deck; and active rain closes it into a heavily self-shadowed front. Rays travel through each profile's defined cloud base and top, so formations show parallax and depth rather than appearing painted onto the sky dome. Sparse rays reject clear weather before reading volume data, while occupied slices use one bounded sunward density probe, ambient height fill, shaded interiors, and restrained sun-facing highlights in the existing single sky draw. Stable world-seeded weather time gently warps only internal detail while the same prevailing and seasonal direction field used by gameplay wind carries formations visibly with the wind at normal speed; render uniforms interpolate that authoritative career clock between fixed simulation samples, while short gusts, instantaneous direction changes, time-speed changes, and rain-event seed changes do not reproject, vertically slide, or teleport formations during accelerated play. Sky color, cloud cover, cloud tone, and open-ocean color share one smooth seasonal weather mood: summer is clearer and bluer, winter is greyer and heavier, autumn is muted without being stormy by default, and rain/storm states visibly darken both sky and ocean. The existing single ocean shader also consumes the same wind and active-rain context: wind direction carries coastal geometry, sampled normal detail, and production raymarched waves downwind, while wind and rain strengthen wave motion, intermittent shoreline breakers, and short beach swash and reduce shallow-water clarity; season alone changes palette and lighting rather than wave energy. Cloud, rain, and water animation remain driven by simulation career time, so they advance with the calendar and freeze when the game is paused. The event clears any remaining active fires as weather without awarding firefighter suppression credit or restoring burned fuel.

## Vegetation and Forest Identity

- Vegetated tiles carry deterministic vegetation state that includes age, canopy cover, and stem density, so forests can look denser without changing the underlying simulation grid.
- Initial woodland distribution responds to long-term moisture, elevation, rendered slope, distance to water and coast, prevailing-wind exposure, lee shelter, drainage, and local ridge/gully curvature. Seeded broad stands, broken edges, and retained clearings replace uniform per-tile placement; exposed windward ridges, steep rock, and coasts stay thinner while sheltered lee slopes and drainage terrain carry denser structure.
- Coastal terrain reads primarily as low scrub rather than a tall-tree perimeter, grass structure never emits tree models, and tall-tree instances overwhelmingly represent authoritative forest tiles or limited woody scrub/riparian accents. Broad wind-face response keeps neighbouring parts of the same slope visually consistent while protected inland valleys concentrate coherent woodland.
- Tile type, static moisture, vegetation age, and fuel remain authoritative. Site quality shapes deterministic canopy/stem structure and recruitment, while exact tree transforms and model variants remain render-only and never add hidden fire modifiers.
- Campaign vegetation maturity is tuned for the 20-year run rather than real-world botany: grass/floodplain mature in about one year, scrub in about two years, and forest reaches full gameplay fuel/canopy maturity in about five years.
- Deciduous tree foliage uses one continuous render-only annual cycle across detailed models and impostors: oak, maple, birch, and elm shed through autumn, remain at the existing sparse rendering floor across the winter year-wrap, and leaf out gradually during spring before reaching full summer foliage. Pine remains evergreen, while scrub retains 55% of its foliage during winter dormancy. Per-tree phase offsets preserve local variation without changing the annual rate, simulation state, fire behavior, saves, or seasonal colour palettes.
- FX Lab contains isolated grass-fidelity prototypes that combine authoritative grass coverage and rendered terrain height with the gameplay camera, scene depth, wind, and lighting. The aggressively optimized Volume Clumps profile rejects scene hits that do not agree with visible grass terrain, derives a central-difference terrain gradient, and backtracks only through the local canopy instead of spreading samples across the whole world box. Adaptive sampling targets at most 0.065 world units per opacity contribution with a four-sample minimum and the existing projected 96/64/40 ceilings; packed height and inward-feathered coverage remain continuously sampled at occupied steps, while conservative non-grass skipping and per-ray cached fields preserve the 60%-resolution performance profile. The reduced grass layer stays linear until the full-resolution scene and grass composite is tone-mapped once. March Work reports terrain-backed executed work, Sample Spacing exposes capped grazing rays, final local grass length is capped at 0.6, and separate wind-response and wind-speed controls isolate motion shimmer. A separately selectable WebGL2 PCG SDF Blades comparison retains its shallow 64-step ceiling and explicit Volume Clumps fallback. The main Graphics settings expose Volume Clumps as a persisted, default-off Volumetric Grass option: campaign grass length combines current authoritative grass fuel with a smooth winter/spring/summer/autumn growth envelope, while curing colour uses the same climate-dominated blend of current global dryness and local tile moisture as terrain colour. Fuel-only fire updates refresh the small property texture without rebuilding terrain geometry, and disabling the option submits no campaign grass passes. On budgeted maps, deterministic native scrub coverage reserves up to 16% of the unchanged shared model ceiling, capped at 4,096 instances; the same number of forest models moves to the existing low-poly coverage renderer. Procedural scrub is retained only when its GLB asset is unavailable or that bounded reservation overflows. Final campaign appearance, motion, compiler, and same-hardware GPU acceptance still require supplied live captures.
- Strategic vegetation growth is processed in deterministic seasonal block batches, not literal daily full-map ecology. Low block budgets must still catch every region up over elapsed career time, while visual terrain refreshes are batched until changes are meaningful.
- Forest expansion should be visible over a few quiet growth seasons; open vegetated tiles and suitable non-rocky bare lowlands can recruit into young forest from runtime tree suitability and seed pressure instead of being locked to initial biome classification.
- Forest stands are assigned one dominant tree identity from pine, oak, maple, birch, or elm. Large forest areas can also contain 1-2 clustered secondary species so they read as stands instead of noisy per-tile mixes.
- Broad environmental bias already shapes composition: drier or higher terrain leans pine, wetter or lower terrain leans elm, and other hardwoods fill the middle ground.
- Tree identity currently drives mapgen readability and rendering variety first. It is not yet a separate fire-fuel model.

## Design Intent

- Cooler/wetter periods allow backburns and controlled fuel management (firebreaks).
- Cooler periods also support lobbying, training, and procurement.
- Hot/dry periods make spread more aggressive and raise multi-front risk.
- During hot periods, most attention is on active response and containment.

## Fire Behavior (Gameplay Perspective)

- Fires spread based on local heat, fuel, moisture, wind, elevation, and neighboring fires.
- Tile type does not add hidden ignition/spread modifiers; differences between forest, house, grass, roads, and other terrain should come from explicit fuel profiles, current fuel load, moisture, heat, wind, elevation, suppression, and non-ignitable fuel values.
- In the campaign, vegetation age controls current available fuel up to the tile type's `baseFuel` cap; SIM Lab treats vegetated tiles as fully fueled from the active fuel profile for direct tuning comparisons.
- Elevation biases heat transfer: uphill cells ignite more easily than downhill cells, while flat terrain remains neutral.
- Terrain shapes wind through a deterministic source-edge propagation approximation derived from the current global wind direction. It allows clamped ridge deflection, valley channeling, lee shelter, and mild wake turbulence without becoming a persistent fluid simulation.
- Spread should be reliable in high-risk conditions; low-risk periods should still allow controlled burns.
- Campaign random ignitions require viable incident weather, so low or moderate forecast risk can still be shown without creating false-alarm fires that immediately fizzle. Manual, debug, controlled-burn, and SIM Lab ignitions can still test low-risk fire behavior.
- The Fire Ignition Events runtime toggle gates new campaign fire starts and heat/spread-scheduled ignition events, allowing no-fire runs that evaluate forest and town growth without burning the region.
- The player should feel the difference between a mild year and a severe year.

## Resource Management

- Budget is allocated yearly based on performance.
- Every generated town starts with a settlement-owned water tower reserved before roads and houses. Non-HQ towers occupy the town seed as a civic generation anchor, with a compact four-way road loop and outward street arms built around the reserved footprint; the HQ keeps the firebase at its center and places its tower at the nearest deterministic clear civic-hub site. It is rendered as a procedural 3D structure with legs, per-leg concrete footings, struts, ladder, side pipe, cylindrical tank, and conical roof; uneven terrain is grounded with individual concrete piers rather than a full-width slab. Tower visuals use the 10 m tile scale: a small town water tower is roughly one tile wide and about 18-25 m tall. Water towers are finite local reservoirs, not terrain water: stopped trucks inside the tower service radius can refill from stored water, draining the tower, while dry weather restores only a slow trickle and active seasonal rain restores water much faster. V1 towers do not provide passive suppression, upgrades, destruction, construction/procurement gating, or manual targeting.
- Resources:
  - Firefighters and trucks (limited capacity).
  - Training upgrades (speed, power, range, resilience).
  - Firebreak construction.
  - Special policies or equipment (unlock-driven).

Design intent:
- Early years: constrained resources, slower response.
- Late years: larger capacity but higher threat.

## Units and Tactics

- Trucks carry crews; crews are not independently commanded.
- Firefighters operate hose streams; trucks carry crew and water but do not directly spray. A truck can move only with at least one crew member to drive, operates one hose with at least two crew, and can operate a second hose only with at least four crew plus Dual Line Operations. Crew must board before movement orders execute, then disembark into deterministic roles before suppression begins: the driver stays hidden in the truck, nozzle operators work the active hose lines, and extra crew become pump-side or hose-assistant support rather than creating extra streams. Third, fourth, and fifth crew members reduce boarding/disembark downtime, with fourth and fifth crew adding small hose-range/handling boosts.
- Squad commands split player-owned truck placement from deployed firefighter tasks. Move, Deploy, Relocate, and Recall decide whether trucks board crew, drive to the player formation, disembark, or clear tasks; Suppress, Contain, Backburn, and Hold Fire only control what deployed firefighters attempt from the truck's current placement. Fire tasks and stance changes must not reposition trucks, except Defensive may retreat when the truck tile is directly unsafe. Out-of-range or not-yet-deployed fire tasks report readiness alerts instead of solving a new truck position.
- Formations allow tactical positioning.
- Deployments should be fast, readable, and predictable.
- Unit and vehicle movement should consume the full movement budget implied by current effective game speed, terrain movement costs, and character/training/progression speed upgrades. Active-fire runtime caps may lower fire work and calendar advancement, but response vehicles should still move at the effective game-speed movement budget. Rendering should interpolate from the previous committed position to the current committed position every frame, so high-speed or upgraded movement reads as continuous travel instead of one-tile step pauses.
- Response trucks strongly prefer connected road, base, and bridge edges. Entering, leaving, or travelling outside that network carries a sixfold off-road cost before terrain type is applied, while rendered incline angles add symmetric penalties at 12, 18, and 28 degrees. Non-bridge segments at or above 38 degrees are impassable even when marked as roads. When the exact ordered tile cannot be reached, a truck stops at the deterministic closest reachable tile and reports that fallback; firefighters retain their existing foot traversal, and civilian evacuation remains road-only.
- Response trucks use one-truck occupancy on road and bridge tiles. A moving truck reserves each road tile before entering it, waits on its existing route when another truck occupies or has reserved the next tile, and resumes after that tile is vacated; temporary traffic does not trigger path replanning. Deterministic unit order resolves simultaneous claims, while bases and off-road formation placement retain their existing multi-unit behavior.
- A firetruck's engine/siren loop is eligible only during fire season while the unpaused truck actually changes position across road or bridge tiles. Stationary trucks, paused simulation, off-road or base travel, non-fire seasons, and firefighter movement remain silent; the existing active-fire proximity and spatial-audio rules still apply.
- Fire engines show alternating red/blue double-flash roof lights throughout a confirmed active incident: incident time plus `burning` fire activity is the authoritative rendering gate. The lights remain active while fielded appliances move, stage, suppress, deploy crew, or retreat, including while simulation is paused; hidden strategic fires, resolved incidents, and ordinary non-incident deployments remain dark. FX Lab retains its dedicated preview override for isolated visual tuning.
- In 3D, hose streams should read as gravity-affected ballistic arcs; when terrain or structures mask the target, the visual stream should lift its attack angle and impact the first visible obstruction instead of drawing a straight-through cone. Supply hoses should appear only for active nozzle operators and read as lightly slack, unfurled hoses between the truck and nozzle rather than taut lines to every deployed firefighter.

## Tactical Evacuation

- Town evacuation is a tactical commitment, not an abstract warning ladder: the player selects a town, picks a road-reachable evacuation destination, previews the exact initial route, then issues the order.
- The route preview is not advice. The player judges the route from visible fire, roads, terrain, town layout, traffic, distance, and fire spread direction.
- The game validates only basic feasibility and must not show route safety scores, recommended points, best routes, alternatives, ETA, survival odds, heat ratings, congestion forecasts, or warning copy that tells the player a route is bad.
- Civilian evacuation uses representative vehicles rendered as varied civilian cars. Vehicles follow the locked route, occupy road slots, queue behind blockers, accumulate heat exposure, burn under sustained or extreme exposure, kill occupants when destroyed, and leave road obstacles that reduce or block capacity.
- Evacuation vehicles use the same smooth route-motion contract as response vehicles: simulation owns route progress, occupancy, heat exposure, and blockage, while rendering interpolates continuous previous/current positions between simulation commits.
- Completed evacuation vehicles remain staged at the selected destination until the player orders evacuees to return home. Non-town destinations visibly park vehicles off-road near the evacuation point; destination towns host evacuees off-map instead, creating over-capacity approval pressure for both the origin and host towns while evacuees stay there. Return-home uses the same locked route in reverse and keeps the same queueing, blockage, and heat exposure rules.
- Evacuation outcomes update town population counts and should feed existing approval/scoring hooks where available.

## Progression and Unlocks

- Tech-tree progression is campaign-scoped and resets for every 20-year run.
- Assisted extinguishes award command experience. Each command level opens a deterministic draft of up to three currently reachable tech nodes; the player selects one node or one additional rank. The existing level curve continues beyond level 10 and drafts stop only when every authored node is at maximum rank.
- Every node has stable graph metadata: branch, tier/order, prerequisites with minimum ranks, maximum rank, rarity, draft weight, effects, and granted capabilities. The eventual visual tree is a roadmap for the full graph and current draft state; it does not bypass draft acquisition.
- The initial graph has four branches:
  - Awareness gates Field Mapping, Weather Instruments, Topographic Survey, Moisture Analysis, Thermal Imaging, Dispatch Tracking, and Aerial Reconnaissance.
  - Operations contains Rapid Response, Fireline Training, Extended Lines, and Air Support.
  - Logistics contains Quick Connects, Tender Upfit, and Dual Line Operations.
  - Policy contains Academy Subsidy and Fuel Break Grants.
- Runtime information is genuinely capability-gated: the minimap is hidden until Field Mapping; each analytical map mode is separately unlocked; all player-facing wind data is hidden until Weather Instruments; unit markers require Dispatch Tracking; and Satellite requires Aerial Reconnaissance. These gates affect information access only and never disable the underlying simulation.
- Fire knowledge is separate from authoritative fire simulation. Fires can ignite and spread before the player knows about them; watch towers, nearby towns/units/assets, and large visible smoke/fire thresholds reveal suspected or confirmed reports without changing fuel, heat, wind, moisture, suppression, scoring, or spread. Watch towers are player-sited town facilities: new placement is maintenance-only, clear high ground within the town service boundary grants up to 50% more detection radius, and distance from roads increases quoted cost. A cursor-following quote shows cost, access surcharge, 90-day construction time, radius, elevation benefit, or the authoritative invalid-site reason. Footprint-aware validation rejects roads, structures, edges, and cliff faces using the actual four leg positions. Towers support eight visibly taller levels with doubling prices; upgrades may start in any phase, take exactly 90 calendar days per level regardless of fire activity, and provide no detection while underway. Clicking a tower in the 3D world opens its owning Town and Watch Tower facility interface. Selecting or placing a tower projects its authoritative effective detection radius over terrain. Grid-aligned procedural towers stand on independently grounded concrete piers directly beneath their legs. Unknown fires do not appear as normal fire alerts or non-thermal minimap markers, but in-frame 3D fire visuals still render from the authoritative simulation. Thermal imaging remains the capability-gated analytical exception that can expose raw heat.
- Unlocks should be meaningful but not invalidate core systems. Ranked numeric perks retain diminishing returns and authored caps.

## Backburning (Planned)

- Player can set controlled burns preferably in low-risk conditions.
- Backburning is a preventative tool, not a free win.
- Ideally permitted based on climate thresholds, not hard seasons.

## UI/UX Goals

- Player-facing event notifications use a typed, extensible bottom-center toast system with event type, category, severity, details, stable deduplication identity, and an optional spatial focus target. Players can persistently enable or disable individual notification types independently from event-source and pause settings. New-fire-front alerts are the first migrated type: suspected reports use amber warning styling, confirmed reports use red critical styling, and the only actions are focus and dismiss. Toasts auto-fade after six visible seconds, pause while interacted with or while the document is hidden, and stack to three without creating a history tray. Their normal edge offset is raised above active command controls only when the toast and control bounds actually overlap.
- Fire alerts are incident/front-based rather than tile-based. Four-directionally connected active fire components retain persistent report lineage while they grow, split, merge, or rejoin; only a genuinely disconnected newly detected front emits another alert. Lineage closes when fire activity becomes idle, and suspected-to-confirmed confidence changes may update an existing visible toast without creating or resurrecting another alert.
- Clear, minimal HUD for time, risk, available budget, and unlocked field intelligence. Runtime minimaps use mutually exclusive terrain, satellite, topographic, moisture, and heat base modes, with the widget and each mode exposed only after its corresponding tech node is owned. Terrain, topographic, moisture, and heat are analytical raster modes; Satellite is a cached top-down 3D world capture refreshed on first selection, visual rebuilds, and sparse multi-day cadence rather than every fire or unit tick. Unlocked wind and unit indicators remain tactical overlays for the analytical modes, while Satellite stays an aesthetic world-recognition view without HUD, label, wind, or unit overlay markers. Suspected fire reports use approximate, visually distinct markers until confidence improves; confirmed reports graduate to normal fire alert behavior. Minimap wind overlays show the global prevailing/current wind plus sparse propagated local wind barbs so players can read ridge deflection, valley channeling, and sheltered downwind wakes; barb length reflects local speed, with calm samples drawn as dots.
- The 3D top-left operational summary is one widget with three run-local modes and a stable header: Main Menu stays on the left, Score and Budget stay centered, and dock-compatible pin/`x` controls stay on the right. Collapsed shows only that header; hovering it or activating its headline metrics reveals Compact command-level progression, draft status, progress, and owned tech; Full is the pinned state and adds the scoring multipliers and active/extinguished/property/lives counters. As with the right dock, unpinning or selecting `x` collapses the widget, pinning restores Full, and an unpinned Compact view can be toggled from the headline metrics. A new run resets the widget to Full, and other right-dock widgets retain their existing collapse behavior. Large connected burn areas retain a 4,096-instance primary flame ceiling, with proportionally larger cross-slice and front-segment budgets, before adaptive overload scaling reduces density.
- Persistent screen widgets in the 3D runtime—the top-left operational summary, right-side dock, and bottom command tray—share a foreground UI layer above world-anchored labels, connectors, and unit hover cards. Modal, loading, notification, and developer-diagnostic surfaces retain their dedicated higher layers.
- New-campaign Terrain setup displays the numeric seed separately from the share code. Independent Randomise Seed and Randomise Sliders actions let the player change either source of terrain variation without changing the other, while the share code continuously encodes both the seed and all exposed terrain variables. Slider randomization preserves the selected map size. Fuel-profile tuning is not exposed in campaign setup; existing configured overrides remain unchanged and dev tuning stays in SIM Lab.
- Operational interface typography uses self-hosted Barlow as the primary UI font at weights 400-700 so small HUD labels, squad names, buttons, statistics, and dense Fire-phase controls stay readable and technical without becoming futuristic. EMBERWATCH title art and major cinematic headings may keep a separate display treatment, but normal HUD, panels, commands, labels, and values should remain Barlow.
- Banner / border shows "season" by color and decals (TBC) along with key metrics (budget, year etc)
- Top right of screen - Forecast graph shows rolling 90-day fire risk with current-day marker.
- Bottom right of screen - "Announcements" at key events ie a speech bubble from a News Station, Weather Presenter, Financial Advisor etc
- Bottom middle of screen - Debug overlays exist for tuning and dev validation.
- Dev-facing SIM Lab exists for controlled fire-behavior tuning: selectable scenario templates run on a denser 128x80 grid, with terrain painting, painted firefighter suppression markers, local saved/loaded test scenarios, a cell-state legend/symbol overlay, fuel profile sliders, and wind, temperature, moisture, risk, and incident-speed preset controls with explanatory tooltips. Firefighter markers maintain a hose-reachable defensive wetness field and auto-spray nearby hot or burning cells using default firefighter radius, hose range, and power. SIM Lab speed mirrors the game's incident-time tuning surface on the same fixed 0.25s incident tick, adds `0.5x` and `1x` lab convenience options, and is capped at `1x`; both SIM Lab and in-game incidents intentionally pace fire-kernel spread below incident clock time so active fires remain tactically readable at the slow presets. The Plain + Road template uses a one-tile road gap so it matches the in-game road scale while still testing fire jumps across non-flammable cells. Fuel profile slider edits apply immediately, auto-save as local SIM Lab drafts, survive saved-scenario loads, and can be copied as a complete `src/config/fuelProfiles.ts` defaults file when ready to promote into source. The `windFactor` slider is displayed as Windbreak: `0` is open/no blocking, `1` is strong wind obstruction.
- Dev-facing FX Lab includes scripted fire, hose, fire-engine emergency lights, shoreline, river/waterfall, house lifecycle, construction dust, and shared sky/cloud/rain weather scenes so rendering-only effects can be verified without waiting on campaign timing and weather tuning stays mirrored with the 3D game view. All scenarios share an instant-loading deterministic showcase map with coherent ocean, beach/cliff coast, upland lake, descending river/waterfall/estuary, varied landforms, vegetation, roads, bridge, base, structures, ash, and firebreak terrain. Dev-only land stamps may raise, lower, flatten, or replace editable surface/vegetation regions while protecting water topology and infrastructure; versioned JSON presets deliberately export/import those edits, and Reset Map restores the canonical showcase without changing FX controls. Its weather playback can lock to the rain event, cycle through the year, or hold fixed winter/spring/summer/autumn cloud states for comparison.
- Bottom command tray is the primary unit control surface and remains anchored to the browser viewport's bottom-left corner across resizes: five fixed squad slots map to keys 1-5, HQ-created squads appear before dispatch, and unstaffed/empty slots stay visually faint. Pressing a squad hotkey or clicking its tray slot works at any time without first opening HQ: a fielded squad becomes the active command unit, while a squad with available trucks at HQ becomes dispatch-ready for the next terrain order. The active squad detail panel mirrors the squad truck layout in compact form, uses status labels such as `At HQ`, `En route`, `Boarding`, `Deploying`, `On fireline`, `Withdrawing`, and `Holding`, and shows trucks, aggregate water/crew/status, placement mode, fire task, stance, and dispatch formation controls. Right-click is the terrain order gesture: a short right-click issues the active placement and fire task at the clicked tile, while holding or dragging right-click projects the chosen Line, Wedge, or Arc formation onto terrain so mouse direction controls facing and mouse distance controls width before release commits the order. While projection is active, camera panning/rotation pauses so the player can tune the formation without the terrain moving underneath the cursor. Pending HQ squad dispatch uses the same quick placement and projected formation placement rules. Normal 3D unit labeling is squad-level: one distinct squad marker with a leader line represents each occupied command-unit squad, while individual trucks keep only minimal selection feedback unless a focused detail view needs them. During camera motion, each billboard and its leader line update in the same frame without screen-space easing; world-anchor smoothing is reserved for actual squad movement.
- Base Ops is a headquarters facility on its owning town, not a separate selectable settlement identity. Every town uses the same Town panel structure for settlement facts, alerts, approval, evacuation, and town-level actions; functional buildings appear in a generic Facilities section. The HQ town banner and Town panel show an `HQ` badge, and selecting the HQ facility opens a sidecar for recruitment, training, persistent squad roster management, and squad dispatch. Non-HQ towns expose town commands such as evacuation and functional facilities such as water towers and watch towers through the same sidecar pattern.
- HQ squads are five persistent named truck groups, managed in the HQ sidecar through compact drag/drop squad rows rather than wide roster rows. The Squads tab is split into `Squad Trucks` for assigning trucks to squads and `Truck Crew` for assigning recruited firefighters to the currently selected squad's trucks. Each squad row has five horizontal vehicle slots; filled slots show compact crew dots and available/fielded water where authoritative truck water exists, while open slots stay faint and visibly empty. Assignment views use firetruck and firefighter icons for compact scanning; firefighter names stay out of the main compact grid and are exposed only through selection/tooltip detail. Firefighters inherit squad participation through their assigned truck rather than becoming direct squad members. Truck squad membership can only change while the truck is available at HQ; crew assignment remains a maintenance task and respects truck capacity. Recruited trucks default into the most empty squad with an open vehicle slot, and recruited firefighters default into the most empty available truck with crew capacity. Partial squads may be dispatched to real map locations. Dispatch is one squad at a time through the HQ facility sidecar and bottom command tray, with initial `Line`, `Wedge`, and `Arc` placement behavior. The HQ recruit tab shows available budget, recruit purchase prices, and current truck/crew counts so recruitment feedback is visible immediately after purchase. Training remains selected-roster-unit based until the broader squad-level versus unit-level training model is resolved. Squads remain where ordered until moved again; autumn rain and winter rollover issue return-to-HQ orders so trucks visibly drive home before becoming editable again.
- Right-side runtime dock stays focused on widgets such as fire risk, minimap, time, and contextual information rather than duplicating unit command ownership.
- 3D run exit controls should be compact and contextual: use a single Main Menu action inside the command/progression counter instead of a separate top header that reduces world viewport space.
- The 3D run command/progression ledger lets players jump between connected active firefronts from the Active Fires row without changing unit selection or command mode; these focus moves should preserve map awareness with eased camera travel, using only modest lift for distant fronts and staying quick for nearby fronts.
- The 3D run camera should stay above the terrain and keep pan targets inside the playable map so normal orbit and pan controls never expose the underside of the world.
- Game-over and manual End Run flows stay on the 3D runtime and show a dedicated end-run summary placeholder with final score, reason, seed, year, and New Run/Main Menu actions.
- New-run map preparation should use a full black loading screen with a centered tip/placeholder graphic pairing that cycles every 10 seconds, a bottom-centered progress bar spanning two-thirds of the viewport, and one compact debug state line for stage-specific detail. Expensive road routing and finalization must identify their active substep; finalization reports morphology, vegetation caching/seeding/composition, each pre-growth year, tile and campaign fuel initialization, terrain-color rows, state publication, and diagnostic capture. Parallel 3D asset preload must show active rather than last-completed families, individual tree/audio file progress, download/parsing/geometry-preparation phase, a live elapsed heartbeat, failures, and unit-weighted completion. Tree GLTF work is limited to two concurrent model requests and yields a browser frame after each prepared model so clustered parsing/cloning cannot starve the overlay. Asset work occupies 75-90% of startup progress; the reserved final band identifies renderer initialization, synchronous terrain/world construction, runtime finalization, and first-frame shader priming with a browser paint opportunity between stages. The loading screen remains stacked above the mounted 3D runtime until first-frame preparation completes. Renderer initialization separately retains tree-impostor-atlas construction time. Terrain construction exposes persistent perf-overlay and structured console diagnostics for assembly, inland-water cutout, normal generation, materials, vegetation, structures, and water. Inland-water diagnostics further separate cutout-domain construction, source-triangle clipping, seam construction, terrain conformance, skirt emission, and buffer finalization, with source/cut/retained/skirt/output geometry counts so hydrology geometry growth can be distinguished from unrelated startup work. Full-resolution clipping and canonical seam conformance use cell-bucketed contour segments and seam vertices rather than rescanning every water-boundary element for every retained polygon edge. Completed finalization, asset, terrain, and startup substeps emit structured console timings so slow loading phases can be isolated after the run.

## 3D Terrain Presentation

- Terrain land color should read primarily from the live sun/shadow rig instead of baking major lighting contrast into the albedo.
- Grass, floodplain, beach, and scorched ground should vary in broad 10-20 tile patches so the land reads as cohesive terrain instead of per-tile checkerboarding.
- Burned ground should use renderer-only scorched coverage to hide square ash boundaries: ash tiles fully surrounded by ash render as complete ash, while edge ash tiles and actively burning vegetated cells use deterministic 16th-cell/triangle char and soot coverage based on neighboring burn-front cells. Protected surfaces such as roads, buildings, water, base, and firebreaks remain readable and are not overpainted by the burn-scar treatment.
- Burning vegetated ground should visibly scorch through fire FX and committed terrain-state changes, with live heat/fuel ground tinting deferred when needed so large fire fronts do not freeze vehicles or camera motion.
- Slopes should subtly desaturate toward rocky/bare tones while keeping terrain type identity readable.
- Biome classification should use rendered slope angle, local single-cell slope, and nearby relief to push steep exposed land toward rocky before forest or scrub; waterfall drop is only a nearby hydrology hint, not the primary steepness signal. Tree suitability stays full on 0-20 degree slopes, tapers through 20-35 degrees, becomes sparse at 35-45 degrees, and is mostly blocked above 45 degrees. Shrub suitability stays full on 0-30 degree slopes, tapers through 30-45 degrees, becomes sparse at 45-55 degrees, and is mostly blocked above 55 degrees.
- The shared final-morphology pass derives continuous rock exposure from rendered slope angle, neighbourhood relief, convexity, erosion wear, and deposition. Biome scoring and the existing `assets/textures/rock_texture.png` mountain material consume that field; absolute hill, mountain, and crag height bands do not independently author exposed rock.
- Dev-facing terrain tools should retain a legacy faceted comparison mode for validating shading changes without affecting simulation data.
- Terrain generation uses a shared grid landmass core for editor previews and `terrain:elevation`, preserving seeded noise, coastline envelopes, public controls, and broad basin placement while treating named archetypes as initial uplift fields rather than authored final mountains. Long Spine supplies a broad curved axial uplift, Massif a concentrated radial uplift, Twin Bay paired uplands around its bay and saddle layout, Shelf a low asymmetric rise, and None no archetype uplift. Narrow analytic mountain ridges, repeated ridge lifts, valley stamps, and authored crag formations do not construct the final silhouette.
- A static deterministic erosion stage priority-floods stable eight-neighbour receivers, accumulates one runoff unit per dry tile, and applies log-normalized flow accumulation with downhill slope as stream power. Incision is bounded by local relief, sea-level headroom, and `0.015` normalized height. Routed sediment deposits where capacity falls, with transport deposition capped at `0.008` per tile; depression routing never carves uphill through the filled surface, leaving lake-prone basins available to hydrology. Two synchronous 42-52 degree talus passes transfer material downslope, and a deterministic 60-degree safety projection prevents pathological cliffs while forming shoulders and footslopes. The pass is generation-time only, deterministic, and particle-free. Settlement and road grading refresh morphology before local biome reconciliation, with a final refresh before world/render samples are published.
- Default islands should avoid circular domes, square map-edge pyramids, raised cut-out slab walls, and narrow coastal cliffs caused by late edge gating. Surface first composes shared low-frequency noise with bounded archetype uplift, basin, and coastline-plan offsets. It then linearly mixes that macro field by a fixed `0.5` toward the Red Blob square-bump target `1 - d`, where `d = 1 - (1 - nx²) × (1 - ny²)`. Coast complexity may add a bounded contour perturbation that vanishes at the centre and perimeter. Fine noise is added after conversion and fades only across the outermost approximately six percent before the exact rectangular perimeter clamp. Archetype coastline plans remain bounded macro offsets rather than fixed final outlines. Water (`hydro:solve`) exclusively chooses the threshold needed to meet Land mass and owns edge-connected ocean membership; it does not reshape Surface and has no secondary sea-level bias. The downstream `terrain:shoreline` phase derives coast distance and beach/cliff/shelf metadata without recomputing sea level, reclassifying ocean cells, or stamping beach and shelf elevations; later mapgen and rendering stages consume that boundary unchanged. Future shoreline morphology must be authored in the upstream Surface before Water calibration. The 3D renderer uses Water's supplied sea-level field for the ocean surface rather than estimating a second level from seabed elevations, keeps below-water terrain as an opaque seabed, and derives a continuous signed visual shoreline from elevation, sea level, coast class, and authoritative ocean connectivity. Only dry land classified as beach should read as playable dry sand; shelf-water seabed stays sandy but becomes cooler and darker with depth beneath continuous light blue-green ocean coverage. The single contextual ocean shader owns shoaling, wind-led waves, intermittent shore-normal breakers, foam, and beach run-up without changing simulation tile classification or adding a second surf render pass, while cliffs suppress landward swash. Coastal geometry and normal maps remain authoritative through the signed swash/shoal band; outside it, the existing coarse 1,456-triangle distant carrier supplies coverage and camera rays only, while fragments reconstruct an MdXyzX height-field hit and normal for lighting. The two fields blend across the shoreline-derived transition. Pixel footprint selects which frequencies and iterations remain without independently flattening all distant normals; as close-scale detail becomes too small, shading blends into a lower-iteration evaluation of the same MdXyzX function at broader world scale, retaining bounded strategic normal and reflection variation while the far horizon remains calmer.
- Static hydrology is a single linear pass over erosion output. Hydrology intensity retains the established trunk threshold, then admits lower-accumulation tributaries only when their receiver chain reaches that trunk or an accepted lake before ocean or sink; terminal branches shorter than three cells are pruned in the same memoized receiver-graph pass. The retained nodes publish deterministic none, ephemeral, stream, and river classes, continuous widths from zero through `0.45`, `0.9`, and `1.35` cells, and their original eight-neighbour downstream receiver. Ephemeral creeks remain traversable, normally burnable terrain and are not gameplay water sources. Streams and rivers keep the existing shallow local bed/surface rules and authoritative non-burnable water mask; diagonal receiver links retain orthogonal connector cells for gameplay raster continuity, but connectors are excluded from the visual flow graph. Lakes remain the largest connected cells whose depression depth was already found by erosion's priority flood, and their masks and surfaces are unchanged. Permanent rendering follows published receiver links with deterministic tapered capsules and round unions sampled at four points per cell, unions unchanged lakes into the same water/cutout contour, and preserves mouths, typed waterfall splits, and canonical terrain seams. Ephemeral presentation instead builds one continuous indexed strip per uninterrupted branch: bounded receiver-tangent curves are subdivided for terrain-height sampling and may shift only toward the local cross-slope low point, never changing topology or adding noise. Headwater strips taper from zero, fade beneath permanent water at confluences, sit below road-deck presentation where both follow a valley, and blend wetness through winter `1.0`, spring `0.9`, autumn `0.25`, and summer `0.05` without cutting terrain or changing simulation state. Rendering adds no routes, camera-dependent hydrology, or cumulative surface solve; older samples without downstream metadata use the legacy raster contour fallback.
- When the inland-water contract supplies typed waterfall spans, the 3D renderer keeps the animated curtain, foam, and mist presentation readable across every adaptive water-quality profile. Fast quality may simplify or reduce the effect, but it must not disable the waterfall presentation; the developer x-ray remains an isolation diagnostic rather than the only visible mode.
- The firebase starts near the center of the main island, but placement scores nearby lowland candidates instead of blindly accepting the exact map center. It should prefer flat, moderate-elevation, roadable ground with nearby vegetated fuel so the base is not stranded on barren high terrain.
- Settlement placement uses rendered slope angle as a first-class terrain fit signal: towns should prefer sites around 12 degrees or lower, tolerate only moderate angle fallback, and reject steep house plots unless mapgen/preplanning can reconcile them into an accessible pad before the run starts.
- Initial map generation guarantees the firebase and all seeded towns share one edge-connected road network. Road strictness remains an internal/debug tuning input, not something players must adjust to avoid isolated towns.
- Intertown roads use bounded multi-destination Dijkstra route search with rendered slope angle, deterministic terrain costs, and failed-attempt reuse to connect each origin to the cheapest valid road, settlement-access, bridgehead, pass, or valley seed currently available. Normal connectors prefer low-angle terrain, switchback along contours on steep climbs when possible, allow only bounded hairpin-context grade discounts for short steep switchback corner steps, skip locally redundant node-to-node recuts, prune redundant short-cycle connector artifacts outside settlement structure zones, use generated non-town junction candidates to avoid repeated full-map pairwise cuts, and reserve bounded mountain-pass fallback for connectivity cases that cannot otherwise be solved. The terrain editor exposes maximum accepted road grade as a settlement-stage tuning control so gradient-related routing failures can be diagnosed without changing route-search code.
- Compound road connectors are applied atomically: failed junction or waypoint attempts must not leave visible dead-end fragments. Mapgen road grading and rendered house-footprint reconciliation keep road surfaces and preplanned building pads accessible; runtime settlement growth must not edit elevation or trigger terrain/water geometry rebuilds.
- 3D town and firebase nameplates should remain readable without drawing through hills or mountains: labels lift along their world anchor until the terrain depth line is clear, lengthening the connector instead of pretending obstructed settlements are in front.
- Each world seed also derives a fictional prevailing wind direction, strength, and variability; that seed climate carries ocean moisture inland so windward slopes trend wetter, leeward rain shadows trend drier, and the resulting static moisture shapes biome, fuel, and later fire-season wind bias. Terrain vegetation derives continuous tree suitability from moisture, elevation stress, slope stress, river/coastal influence, and seeded biome noise, so forests thin probabilistically and patchily instead of ending at a hard contour. Forest remains the fuel category, while pine, oak, maple, birch, and elm are assigned as clustered visual stand patches for 3D assets and canopy color variation.
- The supported 256x256 3D campaign has an opt-in experimental far-tree impostor tier that does not change authoritative vegetation. Complete occupied 64-tile vegetation chunks enter the tier when their tallest tree projects below 18 CSS pixels and return to full models above 24 pixels; chunks outside the camera view stay shadowless instead of restoring off-screen GLB shadow casters. Near and middle chunks retain their GLB geometry, seasonal shaders, burn behavior, and shadows; far chunks render one merged camera-facing draw sourced from a deterministic runtime color and trunk/foliage/mixed-role atlas with four azimuths per loaded variant. Hidden and visible representations receive the same season and burn updates, atlas failure leaves the full-model path active, and FX Lab exposes automatic and forced comparison modes. The tier remains disabled by default until live visual and GPU acceptance passes.
- The terrain editor sequence is Scenario, Uplift, Surface, Sea Level, Erosion, Rivers/Lakes, Biomes, Settlements, Vegetation, and Final. Scenario shows the seeded noise shared across archetypes when other controls are held constant. Uplift isolates the control-shaped uplift and basin-tendency signal on a shared nearly flat surface: a near-overhead view and diverging basin/neutral/uplift overlay make axial, radial, paired, asymmetric, and neutral identities readable, while a subdued coastline-plan tint supplies context only. Surface remains led by that shared Scenario noise, applies the archetype and coastline plan as bounded low-frequency offsets, converts the macro field toward the fixed square-bump island target, and adds fine detail afterward. Sea Level preserves the Surface elevation byte-for-byte, calibrates the Land mass target, and renders connected ocean. Erosion runs the deterministic accumulation, incision, deposition, and talus pass; its comparison toggle flips against the `terrain:elevation` baseline. Rivers/Lakes is a read-only staged result of the eroded terrain and shared Hydrology intensity. Editor steps do not generate automatically: selecting or editing marks the active stage dirty until Generate Preview runs. Dev-only terrain-field overlays expose archetype uplift, flow accumulation, erosion wear, deposition, rock exposure, and biome fields alongside hydrology and road diagnostics; cancellation retains the latest partial preview.

## New Run Configuration (Proposed)

Purpose: expose tunable run constants before each campaign. Defaults are shown, players can keep last-used settings or save a default profile.

### Tabs and Fields

Command Roster
- Chief selection uses compact portrait icons and a large selected-chief detail panel with paired male/female PNG portrait art.
- The roster includes a male/female portrait toggle; random callsigns use first-name pools matching the selected toggle.
- Chief modifiers are shown for budget, speed, power, containment, firebreak cost, and approval retention.
- Campaign difficulty is a clearly selectable, remembered New Game setting with four data-driven levels: Ember, Blaze (default), Firestorm, and Inferno. The selected Chief portrait uses a dedicated procedural GPU flame field and CPU fallback derived from the title screen's IQ noise/fBm approach, behind transparent Chief art and clipped to the rounded picture frame. A uniform near-black backdrop uses the title flame's red-to-white heat palette. Ember disables the flame field and presents a clearly readable field of slow, long-lived sparks over a faint lower glow. Blaze, Firestorm, and Inferno use 4/6/8 detailed emitters plus an increasingly strong continuous wall layer, occupying approximately 50%/75%/100% of frame height; Inferno's full wall prevents the emitters from reading as separate jets. Every profile prewarms its spark population so changing difficulty does not produce a temporarily empty portrait, while higher levels progressively increase spark density and rise speed. The title screen retains its original glyph-masked renderer unchanged. This presentation cue does not add gameplay effects: difficulty changes only the initial budget and starting response-team count; fire, weather, suppression, later-year economy, scoring, and unit performance remain unchanged.
- The normal starting baseline is two response teams, and each complete team contains one truck plus two firefighters. Difficulty produces Ember 4 teams, Blaze 3, Firestorm 2, and Inferno 1, with one complete team retained as the valid-campaign minimum. All difficulty-granted starting trucks belong to the first HQ squad so the complete opening roster can be dispatched as one command group; the other HQ squad slots begin empty.
- Starting budget resolves from the normal base through the difficulty multiplier first (125%, 100%, 85%, or 70%), then through the existing selected-Chief budget modifier without duplicating or changing Chief rules.
- Unit economy + stats (recruit costs, training cost/cap/gains, unit speed/power/radius, truck capacity/board radius, tether distance, formation spacing, movement cost/slope factors, unit loss fire threshold).
- Firebreak cost per tile.

Terrain
- Seed and map size presets.
- The supported campaign world size and rendering-performance target is 256x256 tiles. 512x512 worlds are not a supported gameplay target and currently may crash, so runtime acceptance and profiling should not use them.
- Map generation sliders (forest/meadow/water settings).
- Island archetypes should visibly change the same-seed coastline plan and broad uplift identity; deterministic erosion then derives watershed ridges, valley corridors, catchments, footslopes, and accumulated terrain. The `None` archetype is intentionally neutral, applying no named uplift so developers can judge pure noise and exposed terrain parameters directly. The fast noise/elevation redistribution layer supplies initial conditions for shelves, bays, uplands, and lake-prone basins; literal plate simulation and authored final mountain silhouettes are not design goals.
- Default islands should read as varied single-island regions rather than volcano cones: center position should not imply highest elevation, and uplands should be distributed by seeded ridges, basins, shelves, and valleys.
- Terrain editor previews prioritize instant feedback for uplift, initial surface variation, and sea-level controls using the same fast landmass core as `terrain:elevation`; Erosion and later accurate stages advance the current preview session instead of restarting completed work. Uplift isolates the control-shaped archetype signal, while Surface is noise-led and applies that signal only as a bounded low-frequency elevation, basin, and coastline-plan bias before the fixed `0.5` square-bump conversion. Surface and Sea Level share byte-identical elevation; Sea Level only classifies the result and calibrates its threshold from Land mass. Uplift owns archetype, Relief, Max height, upland distribution, landform alignment, and basin tendency. Surface owns Ruggedness and surface frequency. Erosion owns the existing Hydrology intensity control because it first affects drainage incision and then the downstream river/lake network; Rivers/Lakes is observational. Sea Level owns Land mass and Coast complexity plus the retained advanced coastline modifiers. Border-water falloff, interior land floor, sea-level bias, legacy water level, and carving bypass are not authoring concepts. Production recipes normalize Land mass to 50-70% and Max height to 40-100%; the exact map perimeter remains authoritative ocean. Terrain sharing uses compact `MAP7` codes containing active fields only and rejects `MAP6` and earlier formats.
- Tile fuel profiles (baseFuel/ignition/burnRate/heatOutput/spreadBoost/heatTransferCap/heatRetention/windFactor per tile type); windFactor is retained as the config key but means windbreak strength, where 0 is open terrain and 1 is strong wind obstruction.
- Runtime vegetation growth is one deterministic linear event at the start of each annual growth season. It snapshots woody occupancy before advancing ash to grass, grass or floodplain to shrub or directly to adjacent mature forest, and mature shrub to forest, so no tile can cascade through multiple ecological stages in one year. Establishment is seeded by world and year, uses canopy-weighted eight-neighbour pressure, and is restricted to land with tree suitability of at least `0.32`; water, beach, rock, roads, bases, houses, and active fires remain protected.
- Forest and shrub age, canopy, and stem density mature annually according to site quality. Their fuel carrying capacity grows with vegetation maturity and visible moisture, and fuel closes 16% of the remaining gap without being reduced when a recalculated capacity is lower. New-campaign vegetation starts at 60% of its age-adjusted capacity; type transitions and loaded saves retain their existing fuel and species identity.
- Successful early fire suppression deliberately increases late-campaign danger: in an untreated connected landscape, woody expansion should be visible by years 5-8 and at least 85% of viable land should become forest by years 15-20, with at least 80% of forest near mature structure. Fire resets burned ground to the slower ash-to-grass succession path, so periodically burned landscapes remain materially more open or carry materially less forest fuel.
- Forest instances are deterministic cohorts rather than per-tree simulation state: 20% saplings at 35-55% target scale, 35% mid-sized trees at 65-85%, and 45% mature trees at 90-110%, all multiplied by the tile maturity envelope. On the supported 256x256 campaign, vegetation placement scans the authoritative full-resolution tile field independently of terrain-mesh sampling, reserves one visible tree for every unoccluded forest tile, and uses chunked low-poly trunk/canopy geometry only if the 28,000 high-detail model ceiling cannot cover that reservation. Optional density still prioritizes mature high-canopy forest during thinning; shrub keeps sparse, smaller woody accents. Larger experimental maps retain their existing budgets and do not carry the per-tile coverage guarantee. Canopy density and the existing fire-risk presentation communicate this feedback loop without a new risk UI.
- Community and road generation (town density, compact density-derived starting populations, bridge allowance, settlement spacing, maximum accepted road grade, debug/internal road strictness, guaranteed initial road connectivity, angle-aware intertown routing). Starting towns fill existing road frontage before adding a bounded demand-backed extension, rather than simulating decades of visible road growth before the campaign. The map editor may expose a temporary dev-only skip for expensive road routing so other terrain, hydrology, vegetation, or settlement features can be validated without waiting on intertown road search attempts.

Climate
- Climate params (seasonLen, peakDay, tMid, tAmp, warmingPerYear, noiseAmp, heatwavesPerYear).
- Moisture params (Mmin/Mmax, Tdry0/Tdry1, k0/k1).
- Cooling params (base/alpha/Tref/kMinFactor/kMaxFactor).
- Climate risk mapping (CLIMATE_IGNITION_MIN/MAX, spread base/range, risk weights), forecast window (90).
- Wind model tuning (seeded prevailing wind, seasonal variability, strength base/dryness/temp/year weights, gust).

Fire
- Ignition chance per day, sim speed/tick cadence/rows per slice, render smoothing.
- Fire season taper/min intensity, seasonal fire pacing, and the incident fire-kernel pacing scale used to keep slow active incidents readable.
- Deterministic ranged heat diffusion for short firebreak gaps: 10m gaps can cross in bad conditions, 20m gaps require extreme aligned wind/heat, and 30m gaps require explicit extreme tuning. V1 does not model probabilistic long-range ember spotting.
- Heat diffusion constants, ranged-diffusion thresholds/falloff, windbreak obstruction strength, and heat cap.
- Conflagration boosts.
- Fire bounds padding.
- Elevation spread and local terrain-wind shaping tunables.

Other
- Career/time pacing (career years, days/sec, phase durations, and annual growth-event timing).
- Time controls support persisted preset-button mode and an experimental strategic-time slider spanning 0x-20x in 0.25x steps. Incident mode always uses dedicated `0.25x`, `0.5x`, `0.75x`, and real-time `1x` presets, even when strategic time uses the slider; new incidents default to `0.5x`, entering an incident preserves the strategic slider value, and leaving restores it. `Advance to Next Event` temporarily forces strategic time to 20x before restoring the prior value.
- High-speed strategic time must still detect fire events immediately: when calendar advancement seeds or discovers a detected fire, the sim stops Advance to Next Event, preserves strategic controls for restoration, and enters dedicated slow incident time before any large high-speed fire step can burn past the opening response window. During fire-eligible high-risk weather, idle and active fire work use the same bounded internal detection cadence; detection is polled after each fire substep, and the first alert truncates the accelerated batch and discards its remaining strategic fire backlog so resuming cannot replay pre-alert fast-time spread. `Pause on Fire` controls only whether that incident transition also pauses; disabling it keeps incident time running instead of retaining strategic speed. Strategic fast time is capped at an effective 20x and may be further limited near fire-eligible weather or active fire work by the fire kernel's idle adaptive window. Fire catch-up work is additionally bounded by a per-frame substep budget and may carry deferred fire days across frames only while no incident alert interrupts the batch, so high-speed fire seasons stay responsive instead of processing every backlog substep in one frame. Fire simulation remains tied to simulation speed, but fire FX should interpolate and smooth between previous/current fire snapshots so the visible front does not snap only on fire-kernel ticks. Smoke particle movement, ageing, and both tile and cluster-plume emission accumulate from the same game-speed-scaled visual time, so changing speed changes plume evolution without causing slow-time density buildup from real-time emission. Vehicle movement follows the effective game-speed movement step even when active-fire work applies a lower fire/runtime step, uses the same previous/current visual interpolation contract, and perf telemetry should distinguish requested, effective, movement, and actually applied speed when lower fire/runtime caps are active. The 3D runtime should only hold accelerated simulation for immediate terrain geometry, debug, or structure sync; vegetation, surface-color, and fire-visual batches can defer without stopping movement. Spring settlement growth should replay precomputed road paths and refresh road/structure visuals without forcing a full base terrain mesh rebuild. Fire, annual report, and rain pause behavior can be toggled independently from event source toggles. Run-only diagnostics may temporarily hold simulation and camera motion to capture median GPU sensitivity for terrain, vegetation, structures, fire FX, water, and shadows; this capture is non-authoritative and must restore presentation state without changing gameplay or rendering defaults.
- Economy baselines (base budget, approval min, hectares per tile, initial approval).
- Progression toggles (available upgrades list).
- Debug/perf toggles (simPerf, renderTrees/effects), unlimited money.

### Persistence
- Store “last-used run config” and “saved defaults” separately.
- Provide UI actions: Save as default, Reset to defaults.

### Notes
- Expose gameplay/system constants only; keep render-only constants (colors, zoom ranges, tile sizing) out unless a dedicated Visuals tab is desired.

## Risk Communication

- Forecast visual should map to clear risk tiers (low, moderate, high, extreme).
- Color and UI language must match gameplay impact.
- Risk is a climate severity signal, not a direct guarantee that a random incident will start; campaign random starts are additionally gated by ignition, spread, sustain, and cooling viability.
- Add tooltip or legend for risk meaning and expected behavior.

## Failure States + Win Conditions

- Failure states:
  - Base destroyed.
  - Approval collapse.
  - Region loss exceeds thresholds.
- Win condition:
  - Complete 20-year career with score summary.


## Difficulty Progression Goals

- Year 1: Manageable single-front fire risk.
- Year 5: Two-front risk possible.
- Year 10: Multiple simultaneous ignitions.
- Year 15+: High volatility; proactive planning required.

This can be achieved by:
- Climate warming and drying trend.
- Increased ignition intensity or frequency.
- Slightly reduced moisture damping over time.

## Economy and Scoring

- Performance review each year from Financial Advisor:
  - Land burned, property loss, life loss, containment success.
- Approval impacts next-year budget.
- Career score accumulates across years.

## Systems Inventory (Current)

- Fire simulation (heat diffusion, ignition scheduling).
- Units (truck + firefighter logic).
- Tactical evacuation (town destination selection, locked road routes, representative civilian vehicles, road slot queueing, heat exposure, vehicle destruction, and population/life-loss hooks).
- Climate model (temperature + moisture).
- Map generation (terrain, vegetation age/density, forest stand composition, and deterministic 0-40 year vegetation pre-growth that advances spread and maturity after settlements and roads are reconciled without changing infrastructure).
- Settlements (angle-aware terrain-fit town seeding, constrained-ribbon vs compact street archetypes, compact day-one housing bootstrap, precomputed deterministic 20-year ideal growth queues with future roads hidden until their houses are built, self-contained cumulative prerequisites from successful house plans only, atomic single-extension road trials that are discarded when no valid lot results, approval-gated queue consumption, compact infill/densification, event-style construction catch-up, block-forming road expansion).
- UI system (phase UI, controls, overlays).

## Open Questions

- How should backburning interact with reputation and budget?
- Should there be mid-year policy decisions (e.g., burn bans)?
- How to reward prevention versus reactive suppression?
- Should run events be opt-in choices (risk/reward) or surprise drops?
- How strong can an unlock be before it undermines the climate-driven challenge?
- Should "loot box" events be framed as grants, equipment drops, or political favors?

## Decision Log (fill in)

- Finalized loop:
- Difficulty scaling:
- Backburning rules:
- Economy tuning:
